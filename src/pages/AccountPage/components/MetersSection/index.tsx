import { Typography } from 'antd';

const { Title } = Typography;

export const MetersSection = () => {
  return (
    <div>
      <Title level={2}>Передача показаний счетчиков</Title>
    </div>
  );
}; 